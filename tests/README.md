# Test projects

Tests are split by architectural area so each development task can load only the relevant projects and files.

The current smoke tests prove test discovery only. Behavioural tests are added with the relevant work items.
